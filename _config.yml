title: Card Test Site
description: Minimal Decap CMS test
baseurl: "/site" # Required for GitHub Pages project sites
url: "" # Update to your GitHub Pages URL

# Build settings
markdown: kramdown

# Include admin folder files (config.yml is needed for CMS)
include:
  - admin
